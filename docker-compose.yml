version: '3.8'
services:
  auth-service:
    build:
      context: ./services/auth-service
    ports:
      - "4001:4001"
    volumes:
      - ./services/auth-service:/app
    working_dir: /app
    command: npm run dev
